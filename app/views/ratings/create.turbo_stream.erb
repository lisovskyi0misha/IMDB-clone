<%= turbo_stream.replace 'rate-movie-frame' do %>
  <%= turbo_frame_tag 'rate-movie-frame' %>
<% end %>

<%= turbo_stream.update 'rate-movie-link' do %>
  <%= link_to 'Change rating', edit_movie_rating_path(movie_id: @movie.id, id: @rating.id), class: 'btn btn-light' %>
<% end %>

<%= turbo_stream.remove "rate-movie-link-#{@movie.id}" %>
