<nav class="navbar navbar-expand-lg bg-body-tertiary light-nav header position-fixed top-0 mb-5">
  <div class="container-fluid">
    <%= link_to 'All Movies', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if current_user %>
        <li class="nav-item">
          <%= link_to 'Log out', destroy_user_session_path, class: 'nav-link active', data: { turbo_method: :delete } %>
        </li>
        <% if current_user.admin? %>
          <%= link_to 'Admin', admin_movies_path, class: 'nav-link active' %>
        <% end %>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Sign in', new_user_session_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign up', new_user_registration_path, class: 'nav-link active' %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
